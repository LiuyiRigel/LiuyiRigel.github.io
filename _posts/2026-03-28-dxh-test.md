---
title: '[参考] 多孔介质注热天然气水合物COMSOL 操作'
date: 2026-03-28
permalink: /posts/2026/03/dxh-test/
tags:
  - COMSOL
---

# 二维注热开采天然气水合物 COMSOL 模型（新手可跑通版）

## 1. 创建模型
- Model Wizard → 2D
- 添加物理场：
  - Darcy’s Law（dl）
  - Heat Transfer in Porous Media（ht）
  - Domain ODEs and DAEs（ode）
- Study → Time Dependent

---
# 二维注热开采天然气水合物 COMSOL 模型（新手可跑通版）

## 📌 模型目标

构建一个**二维注热开采天然气水合物的基础数值模型**，实现以下物理过程：

* 多孔介质渗流（Darcy）
* 热传导 + 对流
* 水合物分解（简化动力学）
* 注热驱动分解过程

该模型特点：

* ✔ 可直接运行
* ✔ 耦合关系清晰
* ✔ 易于扩展（适合后续论文升级）

---

## 🧩 1. 创建模型

### 操作步骤

* 打开 Model Wizard
* 选择 **2D**
* 添加物理场：

  * Darcy’s Law（dl）
  * Heat Transfer in Porous Media（ht）
  * Domain ODEs and DAEs（ode）
* Study 类型选择：

  * Time Dependent

---

## 📐 2. 几何（Geometry）

### 操作

* 添加 Rectangle

### 参数

```text
宽度：10 m
高度：5 m
```

---

## ⚙️ 3. 参数（Definitions → Parameters）

```comsol
phi0 = 0.4
k0 = 1e-13 [m^2]

rho = 1000 [kg/m^3]
Cp = 4200 [J/(kg*K)]
lambda = 2 [W/(m*K)]

T_inj = 350 [K]
T0 = 280 [K]

p_out = 3e6 [Pa]
p0 = 5e6 [Pa]

k_react = 1e-5
peq = 4e6

C1 = -5e4
C2 = 0
Mh = 0.016

h = 100
```

---

## 🔧 4. 变量（Definitions → Variables）

```comsol
Ceq = phi0*rho*Cp + (1-phi0)*2600*800
keq = lambda

Qh = (Sh/Mh)*(C1 + C2*T)
q = h*(T_inj - T)

phi = phi0*(1 - Sh)
k = k0*(phi/phi0)^3
```

---

## 🌊 5. Darcy’s Law（dl）

### Domain 设置

```comsol
Permeability: k
Porosity: phi
Density: rho
Viscosity: 1e-3
```

---

### 边界条件

* 左边界：No Flow
* 右边界：

```comsol
p = p_out
```

* 其他边界：No Flow

---

## 🌡️ 6. Heat Transfer（ht）

### Domain 设置

```comsol
Heat capacity: Ceq
Thermal conductivity: keq
```

---

### 勾选物理机制

```text
✔ Conduction
✔ Convection
```

---

### 速度场（关键耦合）

```comsol
[dl.u, dl.v]
```

---

### 热源项

```comsol
Qh + q
```

---

### 边界条件

* 左边界（注热）：

```comsol
T = T_inj
```

* 其他边界：

```text
Thermal Insulation
```

---

## ⚗️ 7. 水合物分解（Domain ODE）

### 添加变量

```text
变量名：Sh
初始值：0.4
```

---

### 控制方程

```comsol
d(Sh,t) = -k_react*max(p - peq, 0)
```

---

## 🧊 8. 初始条件

```comsol
T = T0
p = p0
Sh = 0.4
```

---

## 🔲 9. 网格（Mesh）

```text
Physics-controlled mesh
→ 选择 Normal
```

---

## ⏱️ 10. 求解设置（Study）

```text
Time Dependent

时间范围：
0 → 10000 s

步长：
100 s
```

---

## 📊 11. 结果分析

### 预期现象

#### 温度场

* 左侧高温向右扩散

#### 水合物饱和度（Sh）

* 从左侧开始降低（分解）

#### 压力场

* 从左向右驱动流动

---

## ⚠️ 12. 常见问题与解决

### 问题1：模型发散

```comsol
k_react = 1e-6
```

---

### 问题2：没有对流

检查：

```text
✔ 是否勾选 Convection
✔ 是否填写 velocity field
```

---

### 问题3：Sh 变为负值

```comsol
d(Sh,t) = -k_react*max(p - peq, 0)
```

---

## 🚀 13. 后续升级方向

在此模型基础上可扩展：

### ① 双相流

```text
气相速度 ≠ 水相速度
```

---

### ② 孔隙率动态演化

```comsol
phi = phi0 + alpha*(Sh0 - Sh)
```

---

### ③ 渗透率演化

```comsol
k = k0*(phi/phi0)^n
```

---

### ④ 热-力耦合（THM）

* 添加 Solid Mechanics 模块

---

## ✅ 模型总结

| 特性     | 状态 |
| ------ | -- |
| 可运行    | ✔  |
| 多物理场耦合 | ✔  |
| 数值稳定   | ✔  |
| 适合入门   | ✔  |
| 可扩展论文  | ✔  |

---

## 📌 使用建议

建议按以下顺序逐步提升模型：

```text
单相流 → 注热 → 分解 → 孔隙率 → 渗透率 → THM耦合
```

避免一开始就做复杂耦合导致模型不收敛。



## 方程量纲一致后的表达式

f = -k_react*max((p - peq)/peq, 0)*Sh

## 多孔介质平均流体设置

rho_f = rho_w*Sw + rho_g*Sg + rho_h*Sh

Cp_f  = (rho_w*Sw*Cw + rho_g*Sg*Cg + rho_h*Sh*Ch)/rho_f

-------

# COMSOL 多孔介质传热中“体积热容”设置（含参数示例）

## 📌 结论

```text
无需单独输入“体积热容”，
通过 Matrix + Fluid 自动形成等效热容
```

---

## ⚙️ 参数（Definitions → Parameters）

```comsol
rho_w = 1000      [kg/m^3]
rho_g = 50        [kg/m^3]
rho_h = 900       [kg/m^3]

Cw = 4200         [J/(kg*K)]
Cg = 2000         [J/(kg*K)]
Ch = 2100         [J/(kg*K)]

rho_s = 2600      [kg/m^3]
Cs = 800          [J/(kg*K)]
lambda_s = 2.5    [W/(m*K)]

phi = 0.4
Sw = 0.3
```

---

## 🔧 变量（Definitions → Variables）

```comsol
Sh = 0.4
Sg = 1 - Sw - Sh
```

---

## 🌊 Fluid（流体）设置

### Density

```comsol
rho_f = rho_w*Sw + rho_g*Sg + rho_h*Sh
```

### Heat Capacity

```comsol
Cp_f = (rho_w*Sw*Cw + rho_g*Sg*Cg + rho_h*Sh*Ch)/(rho_w*Sw + rho_g*Sg + rho_h*Sh)
```

---

## 🪨 Porous Matrix（固体骨架）

```comsol
Density: rho_s
Heat capacity: Cs
Thermal conductivity: lambda_s
```

---

## 📦 变量说明

| 变量       | 含义     | 数值   | 单位       | 位置         |
| -------- | ------ | ---- | -------- | ---------- |
| rho_w    | 水密度    | 1000 | kg/m³    | Parameters |
| rho_g    | 气密度    | 50   | kg/m³    | Parameters |
| rho_h    | 水合物密度  | 900  | kg/m³    | Parameters |
| Cw       | 水比热    | 4200 | J/(kg·K) | Parameters |
| Cg       | 气比热    | 2000 | J/(kg·K) | Parameters |
| Ch       | 水合物比热  | 2100 | J/(kg·K) | Parameters |
| rho_s    | 岩石密度   | 2600 | kg/m³    | Parameters |
| Cs       | 岩石比热   | 800  | J/(kg·K) | Parameters |
| lambda_s | 导热率    | 2.5  | W/(m·K)  | Parameters |
| phi      | 孔隙率    | 0.4  | -        | Parameters |
| Sw       | 水饱和度   | 0.3  | -        | Parameters |
| Sh       | 水合物饱和度 | 0.4  | -        | Variables  |
| Sg       | 气饱和度   | 0.3  | -        | Variables  |

---

## 🧠 核心逻辑

[
(ρC_p)_{eff} = (1-φ)ρ_s C_s + φ ρ_f C_f
]

```text
Fluid → 定义混合物性质
Matrix → 定义骨架性质
COMSOL → 自动体积平均
```

---

# COMSOL 多孔介质传热热源项完整设置（统一单位 W/m³）

---

## 总热源（Domain → Heat Source）
```comsol
Q_total = Qh
````

---

## 水合物分解热源（Domain）

```comsol
Qh = rho_h * k_react * max((p - peq)/peq, 0) * Sh * DeltaH
```

---

## 注热边界（Boundary → Heat Flux）

```comsol
q0 = h*(T_inj - T)
```

---

## 参数（Definitions → Parameters）

```comsol
rho_h = 900       [kg/m^3]
k_react = 1e-5    [1/s]
peq = 4e6         [Pa]
DeltaH = -5e5     [J/kg]

h = 100           [W/(m^2*K)]
T_inj = 350       [K]
```
````markdown
# Heat Flux 中 q0 含义与单位问题修正

---

## 1. q0 的含义（Boundary → Heat Flux）

```comsol
q0 = h*(T_inj - T)
````

| 变量    | 含义          | 单位       | 位置            |
| ----- | ----------- | -------- | ------------- |
| q0    | 边界热通量（法向热流） | W/m²     | Heat Flux 边界  |
| h     | 换热系数        | W/(m²·K) | Parameters    |
| T_inj | 注入温度        | K        | Parameters    |
| T     | 当前温度        | K        | Heat Transfer |

---

## 2. Qh 正确单位检查（Domain → Heat Source）

```comsol
Qh = rho_h * k_react * max((p - peq)/peq, 0) * Sh * DeltaH
```

---

## 3. 单位推导

| 项             | 单位    |
| ------------- | ----- |
| rho_h         | kg/m³ |
| k_react       | 1/s   |
| (p - peq)/peq | 无量纲   |
| Sh            | 无量纲   |
| DeltaH        | J/kg  |

---

## 4. 理论结果

```text
kg/m³ × 1/s × J/kg = W/m³ ✔
```

---

## 5. 单位仍错误的原因（逐条检查）

### ❌ 问题1：DeltaH 未带单位

```comsol
DeltaH = -5e5
```

✔ 正确：

```comsol
DeltaH = -5e5 [J/kg]
```

---

### ❌ 问题2：k_react 未带单位

```comsol
k_react = 1e-5
```

✔ 正确：

```comsol
k_react = 1e-5 [1/s]
```

---

### ❌ 问题3：rho_h 未带单位

```comsol
rho_h = 900
```

✔ 正确：

```comsol
rho_h = 900 [kg/m^3]
```

---

### ❌ 问题4：peq 或 p 单位不一致

✔ 必须：

```comsol
p    = ... [Pa]
peq  = 4e6 [Pa]
```

---

## 6. 最终可用表达（完整）

```comsol
Qh = rho_h * k_react * max((p - peq)/peq, 0) * Sh * DeltaH
```

---

## 7. 参数完整写法

```comsol
rho_h = 900       [kg/m^3]
k_react = 1e-5    [1/s]
peq = 4e6         [Pa]
DeltaH = -5e5     [J/kg]
```

---

## 8. 关键结论

```text
Qh → 体积热源（W/m³）
q0 → 边界热通量（W/m²）

二者不在同一位置使用
```



## COMSOL 案例
* https://www.comsol.jp/model/averaging-models-for-effective-thermal-conductivity-in-porous-media-102901